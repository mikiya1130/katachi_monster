export type TypeMonster = {
  base64image: string;
  name: string;
  hp: number;
  gu: number;
  choki: number;
  pa: number;
};

export type TypeHand = "gu" | "choki" | "pa";

export type TypeOutcome = "win" | "lose" | "draw";
